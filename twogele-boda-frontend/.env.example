# Deployed FastAPI (Render)
VITE_API_URL=https://twogele-boda-backend.onrender.com

# Neon Auth Base URL (Neon console → Auth → Configuration)
# Required for login / signup on Vercel and locally
VITE_NEON_AUTH_URL=

# Local backend override:
# VITE_API_URL=http://localhost:8000
